/* global describe, it, beforeEach, expect */

/* TEST GROUP */
describe('A program', function () {
  beforeEach(function () {})

  /* TESTS */
  it('has a feature', function () {
    expect(1).toBe(1)
  })
})
