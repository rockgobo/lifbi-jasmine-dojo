/* global describe, it, beforeEach */

/* TEST GROUP */
describe('Diamond', function () {
  beforeEach(function () { /* do something before each test */ })

  /* TESTS */
  it('has some feature', function () { /* insert some inserts */ })
})
